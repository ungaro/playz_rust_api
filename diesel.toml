# For documentation on how to configure this file,
# see diesel.rs/guides/configuring-diesel-cli

[print_schema]
file = "src/schema.rs"
filter = { only_tables = ["customer","customerinformation","post","asset","post_asset"] }
import_types = ["diesel::sql_types::*", "crate::model::mod::*"]
